<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تفاصيل المنتج - منتج أنيق وعصري</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap');
        body {
            font-family: 'Cairo', sans-serif;
        }
    </style>
</head>
<body class="bg-slate-100 text-gray-800">

    <!-- Navbar -->
    <nav class="bg-white shadow-md sticky top-0 z-50">
        <div class="container mx-auto px-6 py-3 flex justify-between items-center">
            <a href="/" class="text-2xl font-bold text-blue-600 hover:text-blue-700 transition-colors">
                <i class="fas fa-store mr-2"></i>متجر المنتجات
            </a>
            <div>
                <a href="/" class="text-gray-600 hover:text-blue-600 font-semibold py-2 px-4 rounded-lg transition-colors">
                    <i class="fas fa-arrow-left mr-1"></i> العودة للرئيسية
                </a>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container mx-auto px-6 py-10 mb-16">
        <div class="bg-white rounded-xl shadow-2xl overflow-hidden md:flex">
            <!-- Product Image -->
            <div class="md:w-1/2">
                <img src="<%= product.productimageurl %>" alt="صورة المنتج" class="w-full h-auto md:h-full object-cover">
            </div>

            <!-- Product Details -->
            <div class="p-8 md:p-12 md:w-1/2">
                <h1 class="text-4xl md:text-5xl font-bold mb-4 text-gray-800"><%= product.productname%></h1>
                
                <div class="mb-6">
                    <span class="text-5xl font-extrabold text-blue-600"><%= product.productprice %> ج.م</span>
                    <span>
                        <% product.badges.forEach(badge => { %>
                            <span class="<%= badge.color %> text-sm font-semibold px-3 py-1 rounded-full"><%= badge.text %></span>
                        <% }) %>
                    </span>
                </div>

                <div class="mb-6">
                    <h2 class="text-2xl font-semibold mb-2 text-gray-700">الوصف:</h2>
                    <p class="text-gray-600 leading-relaxed text-justify">
                        <%= product.productdescription %>
                    </p>
                </div>
                
                <div class="flex flex-col sm:flex-row sm:space-x-4 sm:space-x-reverse space-y-3 sm:space-y-0">
                    <a href="/edit-product/<%= product._id %>" class="w-full sm:w-auto text-center bg-yellow-500 hover:bg-yellow-600 text-white font-bold py-3 px-6 rounded-lg shadow-md hover:shadow-lg transition-all transform hover:scale-105">
                        <i class="fas fa-edit mr-2"></i> تعديل المنتج
                    </a>
                    <a href="/delete-product/<%= product._id %>" onclick="confirmDelete()" class="w-full sm:w-auto text-center bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-6 rounded-lg shadow-md hover:shadow-lg transition-all transform hover:scale-105">
                        <i class="fas fa-trash-alt mr-2"></i> حذف المنتج
                    </a>
                </div>
                 <div class="mt-6">
                    <a href="#" class="w-full text-center bg-green-500 hover:bg-green-600 text-white font-bold py-4 px-8 rounded-lg shadow-xl hover:shadow-2xl transition-all transform hover:scale-105 text-lg block">
                        <i class="fas fa-cart-plus mr-2"></i> أضف إلى السلة
                    </a>
                </div>
            </div>
        </div>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white text-center p-6 mt-12 fixed bottom-0 w-full">
        <p>© 2023 متجر المنتجات. جميع الحقوق محفوظة.</p>
    </footer>

    <script>
        function confirmDelete() {
            if (confirm("هل أنت متأكد أنك تريد حذف هذا المنتج؟ هذا الإجراء لا يمكن التراجع عنه.")) {
                alert("تم حذف المنتج (هذه رسالة تجريبية).");
                // window.location.href = "index.html"; // للعودة للرئيسية بعد الحذف
            }
        }
    </script>

</body>
</html>